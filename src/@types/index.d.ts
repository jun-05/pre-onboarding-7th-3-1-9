export type SliceState = {
  searchWord: string;
  isLoading: boolean;
  isOpen: boolean;
  itemsLength: number;
  selectIndex: number;
};

export interface ISicks {
  sickCd: string;
  sickNm: string;
}
